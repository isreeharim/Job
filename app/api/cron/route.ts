import {NextRequest,NextResponse} from "next/server";import {fetchRemoteJobs} from "@/lib/job-sources";import {supabaseAdmin} from "@/lib/supabase";
export async function GET(req:NextRequest){if(process.env.CRON_SECRET&&req.headers.get("authorization")!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({error:"Unauthorized"},{status:401});const jobs=await fetchRemoteJobs();let upserted=0;if(supabaseAdmin&&jobs.length){const rows=jobs.map(j=>({id:j.id,title:j.title,company:j.company,location:j.location,url:j.url,description:j.description,source:j.source,published_at:j.publishedAt||null}));const{error,count}=await supabaseAdmin.from("jobs").upsert(rows,{onConflict:"id",count:"exact"});if(error)console.error("Supabase upsert failed",error);else upserted=count??rows.length;}return NextResponse.json({ok:true,found:jobs.length,upserted,checkedAt:new Date().toISOString()})}