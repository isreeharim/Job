"use client";import {useState} from "react";import {supabase} from "@/lib/supabase";
export default function Login(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function login(){if(!supabase){setMsg("Configure Supabase environment variables first.");return}const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:location.origin}});setMsg(error?error.message:"Check your email for the login link.");}return <main className="auth"><h1>Welcome back.</h1><p>Sign in to save jobs and track applications.</p><input placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/><button onClick={login}>Send magic link →</button><small>{msg}</small></main>}