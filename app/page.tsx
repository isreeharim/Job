"use client";import {useEffect,useState} from "react";type Job={id:string;title:string;company:string;location:string;url:string;source:string;publishedAt?:string};export default function Home(){const[jobs,setJobs]=useState<Job[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/jobs").then(r=>r.json()).then(d=>setJobs(d.jobs||[])).catch(()=>setJobs([])).finally(()=>setLoading(false))},[]);return <main className="app"><header><a className="brand" href="/"><span className="brandMark">↗</span>Remote<span>Flow</span></a><a className="alertLink" href="/alerts">🔔 Alerts</a></header><section className="hero"><div className="eyebrow">● REMOTE JOBS FOR FRESHERS</div><h1>Start your career.<br/><em>From anywhere.</em></h1><p>Entry-level remote jobs, internships, graduate roles and junior opportunities — collected in one place.</p><a href="#jobs" className="primary">Explore fresher jobs →</a></section><section id="jobs" className="jobsSection"><div className="sectionHead"><div><div className="eyebrow">FRESH OPPORTUNITIES</div><h2>{loading?"Finding fresher jobs…":jobs.length+" fresher opportunities"}</h2></div><div className="fresherBadge">🎓 Fresher & entry-level only</div></div><div className="grid">{jobs.slice(0,24).map(j=><article key={j.id}><div className="cardTop"><span className="score">FRESHER FRIENDLY</span><span className="source">{j.source}</span></div><h3>{j.title}</h3><p className="company">{j.company}</p><div className="meta">🌍 {j.location||"Remote worldwide"}</div><div className="skills"><span>Remote</span><span>Entry level</span>{/intern/i.test(j.title)&&<span>Internship</span>}</div><a href={j.url} target="_blank" rel="noreferrer" className="apply">View opportunity ↗</a></article>)}</div></section></main>}