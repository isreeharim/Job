"use client";import {useEffect,useState} from "react";import {scoreJob,MatchProfile} from "@/lib/matching";const KEY="remoteflow-match-profile";export function MatchBadge({job}:{job:any}){const[profile,setProfile]=useState<MatchProfile|null>(null);useEffect(()=>{try{const p=JSON.parse(localStorage.getItem(KEY)||"null");setProfile(p);}catch{}},[]);if(!profile)return null;const m=scoreJob(job,profile);if(m.score<15)return null;return <span className="matchBadge">{m.score}% match</span>}