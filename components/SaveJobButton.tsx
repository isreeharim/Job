"use client";import {useEffect,useState} from "react";type Job={id:string;title:string;company:string;location:string;url:string;source:string;publishedAt?:string};const KEY="remoteflow-saved-jobs";export function SaveJobButton({job}:{job:Job}){const[saved,setSaved]=useState(false);useEffect(()=>{try{setSaved(JSON.parse(localStorage.getItem(KEY)||"[]").some((j:Job)=>j.id===job.id));}catch{}},[job.id]);function toggle(){try{const current:Job[]=JSON.parse(localStorage.getItem(KEY)||"[]");const next=saved?current.filter(j=>j.id!==job.id):[job,...current];localStorage.setItem(KEY,JSON.stringify(next));setSaved(!saved);}catch{}}return <button className={"saveButton"+(saved?" saved":"")} onClick={toggle}>{saved?"♥ Saved":"♡ Save job"}</button>}