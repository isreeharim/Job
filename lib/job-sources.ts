import {Job} from "./types";
const sources=[{name:"Remotive",url:"https://remotive.com/api/remote-jobs"}];
const senior=/\b(senior|sr\.?|staff|principal|lead|manager|director|architect|head of)\b/i;
const fresher=/\b(junior|entry[ -]?level|graduate|new grad|fresher|intern(ship)?|trainee|associate|apprentice)\b/i;
export function isFresherJob(job:Job){const text=(job.title+" "+job.description).replace(/<[^>]*>/g," ");if(senior.test(job.title))return false;return fresher.test(text);}
export async function fetchRemoteJobs():Promise<Job[]>{const jobs:Job[]=[];for(const source of sources){try{const res=await fetch(source.url,{next:{revalidate:3600}});if(!res.ok)continue;const data=await res.json();for(const j of data.jobs??[]){const job:Job={id:source.name+"-"+j.id,title:j.title,company:j.company_name,location:j.candidate_required_location||"Remote",url:j.url,description:j.description||"",source:source.name,publishedAt:j.publication_date};if(isFresherJob(job))jobs.push(job);}}catch(e){console.error("Job source failed",source.name,e)}}return jobs.sort((a,b)=>new Date(b.publishedAt||0).getTime()-new Date(a.publishedAt||0).getTime());}