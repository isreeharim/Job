import {Job} from "./types";
export async function sendTelegramDigest(jobs:Job[]){const token=process.env.TELEGRAM_BOT_TOKEN;const chatId=process.env.TELEGRAM_CHAT_ID;if(!token||!chatId||!jobs.length)return false;const lines=jobs.slice(0,15).map(j=>`• ${j.title} — ${j.company}\n${j.url}`);const text=`🔔 ${jobs.length} new fresher remote job${jobs.length===1?"":"s"} found\n\n`+lines.join("\n\n");try{const res=await fetch(`https://api.telegram.org/bot${token}/sendMessage`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({chat_id:chatId,text,disable_web_page_preview:true})});return res.ok;}catch(e){console.error("Telegram notify failed",e);return false;}}
